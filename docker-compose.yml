version: "3.7"
services:
  idea-directory:
    container_name: idea-directory
    build: .
    stdin_open: true
    tty: true
    image: idea-directory-image
    volumes:
      - ./public-idea-list:/usr/src/idea-directory
    ports:
      - 3000:3000

